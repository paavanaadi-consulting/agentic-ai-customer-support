{{- if .Values.mcpKafka.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "agentic-ai-customer-support.fullname" . }}-mcp-kafka
  labels:
    {{- include "agentic-ai-customer-support.labels" . | nindent 4 }}
    app.kubernetes.io/component: mcp-kafka
spec:
  replicas: {{ .Values.mcpKafka.replicaCount }}
  selector:
    matchLabels:
      {{- include "agentic-ai-customer-support.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: mcp-kafka
  template:
    metadata:
      labels:
        {{- include "agentic-ai-customer-support.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: mcp-kafka
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.securityContext }}
      securityContext:
        {{- toYaml .Values.securityContext | nindent 8 }}
      {{- end }}
      containers:
      - name: mcp-kafka
        image: "{{ .Values.global.imageRegistry }}{{ .Values.mcpKafka.image.repository }}:{{ .Values.mcpKafka.image.tag | default .Chart.AppVersion }}"
        imagePullPolicy: {{ .Values.mcpKafka.image.pullPolicy }}
        ports:
        - name: http
          containerPort: 8002
          protocol: TCP
        env:
        {{- include "agentic-ai-customer-support.commonEnv" . | nindent 8 }}
        - name: MCP_SERVER_PORT
          value: "8002"
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          {{- toYaml .Values.mcpKafka.resources | nindent 10 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
