# Docker-optimized requirements
# Excludes packages already in jupyter/scipy-notebook base image:
# - numpy>=1.24.0 (pre-installed: 1.24.4)
# - pandas>=2.0.0 (pre-installed: 2.1.1) 
# - scikit-learn>=1.3.0 (pre-installed: 1.3.1)
# - matplotlib>=3.8.0 (pre-installed: 3.8.0)
# - scipy>=1.11.0 (pre-installed: 1.11.3)

# Core application dependencies
google-generativeai==0.8.0
anthropic==0.34.0
openai==1.51.0
psycopg2-binary==2.9.9
boto3==1.35.0
kafka-python==2.0.2
qdrant-client==1.11.0

# Web framework
fastapi==0.115.0
uvicorn==0.30.6
pydantic==2.9.0
pydantic-settings==2.5.2
python-multipart==0.0.12
websockets==13.1
aiohttp==3.10.5

# Document processing (lightweight)
PyPDF2==3.0.1
python-dotenv==1.0.1
loguru==0.7.2

# MCP Packages
postgres-mcp @ git+https://github.com/crystaldba/postgres-mcp.git

# Note: sentence-transformers is excluded from Docker build as it's very heavy
# If needed for production, it can be added but will significantly increase build time
# sentence-transformers>=2.2.0

# Note: Kafka MCP is handled via Docker using Confluent's mcp-confluent server (Node.js)

# Testing dependencies (optional for Docker)
pytest==8.3.3
pytest-cov==5.0.0
pytest-asyncio==0.24.0
pytest-mock==3.14.0
moto[s3,lambda,ssm]==5.0.0

# Code quality (optional for Docker)
black==24.8.0
flake8==7.1.1
mypy>=1.0.0

# Additional dependencies
langchain>=0.1.0
